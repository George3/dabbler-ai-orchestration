Item: H4
Verdict: refine
Reasoning: The `engine`-only proposal is a strong simplification but introduces a known future risk of collision, which the pre-audit text correctly identifies as an edge case. Using a composite key of `engine + provider` is more robust and handles the realistic scenario where the same engine is offered through different platforms (e.g., Claude via Anthropic vs. Claude via AWS Bedrock). This change avoids a future breaking change at a negligible implementation cost, as both fields already exist. It provides stronger identity guarantees without adding schema complexity.

**Proposed alternative:** Holder identity = `engine + provider`. The conflict-equality predicate must compare both the `engine` and `provider` fields from the `orchestrator` block. The `model` and `effort` fields remain mutable holder-state and are excluded from the identity comparison.

**Why it beats `engine`-only:** It is more future-proof by default and correctly handles the plausible scenario of multiple providers for a single engine type, which the `engine`-only approach acknowledges as a future point of failure.

No additional items.