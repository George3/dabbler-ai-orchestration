**Q1 — ActionRegistry correctness.**
VERIFIED. The `when` predicates correctly constrain `dabbler.setOrchestrator` to `in-progress` sets and make `dabbler.openOrchestratorWriterLog` always available. The new `group: 5xx` ensures deterministic menu ordering. The corresponding test additions are correct.

**Q2 — OrchestratorAccordion HTML cleanup.**
VERIFIED. The `acc-actions` div is cleanly removed from both `renderAccordionEmpty` and `renderAccordionLoaded`. The surrounding template literal structures in both functions remain valid and preserve required elements like the empty-state `acc-link` and the loaded-state `model-sections`.

**Q3 — Dead-CSS removal in `tree.css`.**
VERIFIED. The `.acc-actions` and `.acc-action` rules are cleanly removed and replaced with a descriptive comment. The adjacent `.acc-link` rule, which is still in use, is correctly preserved.

**Q4 — `readCurrentMarkerForWorkspace` async correctness.**
VERIFIED. The refactor correctly replaces all synchronous `fs` calls with their `await`ed `fs.promises` equivalents. The function signature is correctly updated to `async` and returns a `Promise`, and the single internal caller correctly `await`s the result. Control flow, including loop termination and error handling, is preserved.

**Q5 — Test correctness.**
VERIFIED. The primary test case is correctly updated to assert the new total of 16 actions. The new test case is also correct, exhaustively checking the state-based visibility logic for both new actions across all relevant session states.

**Q6 — Anything else risky.**
SUGGEST. The diff header shows multiple `LF will be replaced by CRLF` warnings. This indicates inconsistent line-ending configuration across developer environments. To enforce consistency and eliminate this noise, add a `.gitattributes` file to the repository root.
â†’ **Location**: `/` (repository root)
â†’ **Fix**: Create a new file named `.gitattributes` with the following content:
```
* text=auto eol=lf
```