ISSUES_FOUND

- **Moderate/Correctness** → `ai_router/pull_verifier.py::_has_nested_quantifier` (`quantified = nxt in "*+{"`) → **R1 Finding A is resolved**, but **R1 Finding B is not fully resolved**: the scanner treats any `){...}` as a dangerous outer quantifier, so safe bounded repeats on a quantifier-bearing group like `(a+){2}` or `(ab(c+)d){3}` are falsely rejected, which violates the stated “bounded `{n}` reps” non-false-positive requirement. **Fix:** parse brace quantifiers enough to distinguish bounded `{n}` / `{n,m}` from unbounded `{n,}` and only flag the unbounded outer forms; add unit tests covering bounded-group cases that must remain allowed.