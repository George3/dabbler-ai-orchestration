ISSUES_FOUND

- **MAJOR/Protocol/Location `ai_router/pull_verifier.py:1443-1518`** → Responses that contain `submit_verdict` plus any sibling tool call are still not fully answered: a valid first verdict `break`s before dispatching `non_verdict_calls`, and an invalid first verdict only emits a tool result for `verdict_calls[0]`, leaving extra `submit_verdict` calls unanswered. That recreates the provider-400 path on the next turn for multi-tool/multi-verdict responses and violates the “every tool_use answered” requirement. **Fix:** iterate all tool calls; either require exactly one `submit_verdict` and return matching error `tool_result`s for every extra tool call, or answer every call before terminating.  
- **MAJOR/Security/Location `ai_router/pull_verifier.py:355-373`** → `_NESTED_QUANTIFIER_RE = re.compile(r"\([^)]*[*+{}][^)]*\)[*+{]")` misses classic catastrophic patterns when the quantified inner piece is inside a nested group, e.g. `((a+))+` / `(ab(c+)d)+`, because `[^)]*` stops at the first inner `)`. Those patterns still reach Python `re` and can hang the orchestrator. **Fix:** replace the single-regex heuristic with a linear parser that tracks group nesting plus “contains quantifier” state, or run grep in a timeout/safe-regex sandbox.