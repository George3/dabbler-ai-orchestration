```json
{"verdict":"ISSUES_FOUND","issues":[{"id":"S061-S3-V1-001","severity":"Major","category":"FalsePositive","description":"The switch-to-full router guardrail checks `ai_router/router-config.yaml` relative to `set.root` (`path.join(set.root, ROUTER_CONFIG_REL)`), but D4 requires checking under the workspace/project root. If `set.root` is the session-set folder (as the model naming and other row actions imply), installed routers at `<workspace>/ai_router/router-config.yaml` will still trigger a missing-router warning.","suggested_fix":"Resolve `ROUTER_CONFIG_REL` against the workspace folder/project root associated with the set, not the session-set directory. Pass that root into the command or derive it from `vscode.workspace.getWorkspaceFolder(vscode.Uri.file(set.specPath))`."},{"id":"S061-S3-V1-002","severity":"Major","category":"Correctness","description":"Malformed on-disk `tier:` scalars are not handled consistently with the parser. `switchTier` exits early when `target === set.config.tier`, so values that the parser defaults to `full` (for example `tier: ful`) never get normalized; additionally, `rewriteSpecTier` lowercases the captured scalar before comparison, so case-variant invalid values like `tier: LIGHTWEIGHT` can be treated as `already-target` and yield a false success/no-op even though `parseSessionSetConfig` would still interpret them as `full`.","suggested_fix":"Make the rewriter use the same exact tier validation as `parseSessionSetConfig`/`asTier` (no lowercase normalization unless the parser does it too), and let the helper decide whether a rewrite is needed instead of returning early solely from `set.config.tier`."}]}
```

- **S061-S3-V1-001:** The pure guardrail helper is fine, but the command-level existence check appears rooted incorrectly. That makes the D4 warning noisy and misleading in normal installed-router workspaces.

- **S061-S3-V1-002:** The helper comments explicitly claim parser/rewrite agreement on unknown scalars, but the implementation diverges on malformed values. In those cases the command can either skip a needed canonicalization or report a successful switch without changing the effective tier.