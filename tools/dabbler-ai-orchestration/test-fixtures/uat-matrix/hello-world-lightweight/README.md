# hello-world-lightweight

A trivial hello-world CLI used as the **Lightweight-tier project** in
the Dabbler UAT fixture matrix: its session sets cover every
Lightweight marker state (`lw`, `N/M+`, `v?`, `v+`, verified-quiet).
See `../README.md` for the row inventory.

Run it with `node hello.js [name]`.
